map:{
    ; schemaHtmlFileName = syncworld-schema-maihamadb.html
    ; historyHtmlFileName = syncworld-history-maihamadb.html
    ; coreCraftMetaDirPath = $$DEFAULT$$/syncworld

    ; schemaSyncCheckMap = map:{
        ; url = jdbc:h2:file:./dfprop/syncworld/syncworlddb
        ; schema = PUBLIC
        ; user = sa
        ; password = 
        ; resultHtmlFileName = syncworld-sync-check-result.html
        ; craftMetaDirPath = $$DEFAULT$$/syncworld
    }
}
